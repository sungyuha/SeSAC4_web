hook 란?
리액트의 새로운 기능
클래스 컴포넌트에서만 가능했던 state(상태관리)

hook은 최상위 단계에서만 호출 가능
- 반복문, 조건문, 중첩된 함수 내부에서 호출하면 안도는 것!

CSS
- 기존 CSS 클래스를 이용할 경우, 컴포넌트 별 클래스가 중복되지 않도록 클래스명을 지정해야 한다.
방법1) 이름 규칙 정하기
방법2) CSS Selector

Sass 
- 매우 멋진 CSS
- 스타일 코드의 재활용성과 코드의 가독성을 높여 유지보수를 더욱 쉽게 해준다.

;과 {} 없음

Scss
;과 {} 존재

변수를 만들 때 $ 표기 해줘야 함